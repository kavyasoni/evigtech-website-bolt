# This is the root .gitignore for GitHub Pages deployment
# Source code is in /code directory
# Built files (index.html, assets/, etc) are in root for GitHub Pages

# System files
.DS_Store
*.log

# IDE
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Keep code directory separate (has its own .gitignore)
# Keep built files in root (index.html, assets/, favicon.png, robots.txt) for GitHub Pages
